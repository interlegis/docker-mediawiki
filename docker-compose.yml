moodle:
  #image: mediawiki
  build: .
  volumes:
   - wikiimages:/var/www/localhost/htdocs/images
  ports:
   - "80:80"
  environment:
   - WIKI_URL=http://localhost
   - WIKI_DB_HOST=localhost
   - WIKI_DB_NAME=wikidb
   - WIKI_DB_USER=wikiuser
   - WIKI_DB_PASS=wikipass
